import * as demo from '../../phaser/versions/devices.esc'
const copy = Object.assign({}, demo) as any

export const esElement = {} as any
export const esComponents = copy.esComponents
export const esListeners = copy.esListeners
